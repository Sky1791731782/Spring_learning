<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd">
    <!--
        告诉Spring创建对象
           声明bean，就是告诉Spring要创建某个类的对象
           id：对象的自定义名称，唯一值
           class：是类的全限定名称（不能是接口，Spring是反射机制创建对象，必须使用类）
           spring是把创建的对象放到map中，Spring框架有一个map存放对象的
           Springmap.put(id的值,对象）
           一个bean声明一个对象
    -->
       <bean id = "service" class="Spring_learning.day01.service1.impl.ServiceTestImpl"/>
</beans>

<!--
    spring的配置文件
    1.beans：是根标签，Spring把Java对象称为bean
    2.spring-beans.xsd 是约束文件，和mybatis指定的 dtd是一样的
-->